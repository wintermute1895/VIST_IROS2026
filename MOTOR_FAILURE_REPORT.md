# 电机烧毁事故报告

## 事故信息
- **时间**: 2026-02-24 19:xx
- **受影响设备**: 机器人电机（具体哪个电机？）
- **当时操作**: 准备测试外骨骼遥操控制

## 紧急处理
✅ 已停止所有ROS2控制程序
✅ 已停止linkerta节点

## 可能原因分析

### 1. 多个控制节点冲突 ⚠️ 高风险
**发现**: 系统中同时运行了多个linkerta_node实例
```
进程9147  - 启动于 11:47
进程11648 - 启动于 12:18
进程17844 - 启动于 12:48
进程17846 - 启动于 12:48
```

**问题**: 多个节点可能同时向机器人发送控制命令，导致：
- 命令冲突
- 电流过载
- 电机过热

### 2. 之前分析发现的安全问题

#### 关节限位配置错误 🔴 严重
- **位置**: config/system_config.yaml 第36-42行
- **问题**: 关节限位设为 [-7, 7] 弧度（±401度）
- **风险**: 物理上不可能的角度，可能导致电机堵转

#### 缺少安全保护 🔴 严重
- 无硬件急停集成
- 心跳超时未强制停止
- 无碰撞检测
- 安全控制器被禁用（vist_controller.py）

#### 连接问题 🟡 中等
- 运行中自动重连不停止运动
- 连接健康监控缺失
- SDK错误处理不完整

### 3. 外骨骼遥操特定风险

#### 高频控制 (~250Hz)
- 外骨骼输出频率很高
- 可能导致电机过载
- 缺少速度/加速度限制

#### 无滤波保护
- 用户明确要求"暂时不加滤波"
- 直接透传可能有突变
- 缺少平滑处理

### 4. 可能的触发场景

#### 场景A: 多节点命令冲突
```
linkerta_node #1 → 发送命令A
linkerta_node #2 → 发送命令B (冲突)
机器人 → 尝试同时执行 → 电流过载
```

#### 场景B: 关节限位错误
```
外骨骼 → 发送大角度命令
teleop_bridge → 限位检查失败（配置错误）
机器人 → 尝试超限运动 → 堵转 → 烧毁
```

#### 场景C: 突然启动
```
外骨骼 → 初始位置与机器人当前位置差异大
系统 → 发送大幅度运动命令
机器人 → 高速运动 → 电流过载
```

## 立即行动项

### 1. 硬件检查 🔴 立即
- [ ] 断开机器人电源
- [ ] 检查哪个电机烧毁
- [ ] 检查是否有其他损坏
- [ ] 检查电机驱动器状态
- [ ] 检查保险丝/断路器

### 2. 日志收集 🔴 立即
```bash
# 收集ROS2日志
cd ~/.ros/log/
ls -lt | head -20

# 查看最近的日志
tail -100 ~/.ros/log/latest/ros2.log

# 检查系统日志
journalctl -xe | tail -100
```

### 3. 配置检查 🟡 紧急
```bash
# 检查关节限位配置
cat config/system_config.yaml | grep -A 10 "joint_limits"

# 检查外骨骼配置
cat external_sdk/arm_teleop/src/lbot_teleop/config/teleop_config.yaml
```

## 预防措施（修复后）

### 1. 修复关节限位 🔴 必须
```yaml
# config/system_config.yaml
joint_limits:
  min: [-3.14, -2.0, -3.14, -2.0, -3.14, -2.0, -3.14]  # 合理的弧度值
  max: [3.14, 2.0, 3.14, 2.0, 3.14, 2.0, 3.14]
```

### 2. 添加启动检查 🔴 必须
```bash
# 启动前检查是否有重复节点
if pgrep -f "linkerta_node" > /dev/null; then
    echo "错误: linkerta_node已在运行！"
    exit 1
fi
```

### 3. 添加速度限制 🟡 强烈推荐
- 限制最大速度（如0.5 rad/s）
- 限制最大加速度（如1.0 rad/s²）
- 添加运动平滑

### 4. 添加安全监控 🟡 强烈推荐
- 监控电机电流
- 监控电机温度
- 超限自动停止

### 5. 改进启动流程 🟢 推荐
```bash
# 1. 清理旧进程
pkill -f "linkerta_node"
pkill -f "lbot_driver"

# 2. 等待清理完成
sleep 2

# 3. 验证清理成功
if pgrep -f "linkerta_node"; then
    echo "清理失败！"
    exit 1
fi

# 4. 启动新进程
ros2 launch lbot_teleop teleop.launch.py
```

## 需要回答的问题

1. **哪个电机烧毁了？**
   - 左臂还是右臂？
   - 哪个关节（J1-J7）？

2. **烧毁时的现象？**
   - 有异味吗？
   - 有冒烟吗？
   - 有异常声音吗？
   - 机器人在做什么动作？

3. **当时的操作？**
   - 刚启动系统？
   - 正在运动中？
   - 外骨骼位置与机器人位置差异大吗？

4. **之前有异常吗？**
   - 电机有发热吗？
   - 运动有卡顿吗？
   - 有报错信息吗？

## 后续计划

### 短期（修复后）
1. 更换烧毁的电机
2. 修复所有已知的安全问题
3. 添加完整的安全检查
4. 小范围测试（低速、小幅度）

### 中期
1. 实现完整的安全系统
2. 添加硬件急停
3. 添加碰撞检测
4. 添加工作空间限制

### 长期
1. 建立完整的测试流程
2. 添加仿真验证
3. 建立安全操作规范
4. 定期维护检查

## 联系支持

如果需要技术支持：
- 机器人厂商: [联系方式]
- 外骨骼厂商: [联系方式]
- 电机供应商: [联系方式]

---

**重要**: 在修复所有安全问题之前，不要再次启动系统！