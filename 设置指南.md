# LinkerHand L10 基于视觉的控制系统

## 📋 概述

本系统使用MediaPipe手部追踪和状态机方法，实现了LinkerHand L10机械手的视觉控制。

## 🏗️ 架构

```
摄像头 → 视觉控制器 → LinkerHand驱动 → 硬件
         (MediaPipe +        (CAN总线)
          状态机)
```

### 组件

1. **视觉控制器** (`src/core/vision_controller.py`)
   - MediaPipe手部检测
   - 几何特征提取（捏合比率）
   - 带防抖的状态机
   - 输出目标关节角度

2. **LinkerHand驱动** (`src/robot/hand_driver.py`)
   - CAN总线通信
   - 带优化的硬件控制
   - 缓冲区溢出防护

3. **主入口** (`scripts/run_hand.py`)
   - 系统集成
   - 摄像头捕获
   - 可视化
   - 用户界面

## 🎯 状态机

系统使用基于捏合比率的3状态机：

| 状态 | 捏合比率 | 描述 |
|-------|-------------|-------------|
| **IDLE（空闲）** | > 0.8 | 手完全张开 |
| **PRE_GRASP（预抓取）** | 0.3 - 0.8 | 预抓取姿态 |
| **GRASP（抓取）** | ≤ 0.2 | 捏合/抓取（闭合） |

### 捏合比率计算

```
捏合比率 = 拇指指尖到食指指尖的距离 / 手腕到中指根部的距离
```

这个归一化指标与手的大小和摄像头距离无关。

### 防抖动

状态机需要**连续5帧**检测到相同状态才会切换。这可以防止抖动的转换。

## ⚙️ 关节角度配置

**重要**：你需要在 `src/core/vision_controller.py` 中填入预设的关节角度

### 位置

在 `VisionController` 类的顶部找到这些数组（第30-80行）：

```python
JOINT_ANGLES_IDLE: List[float] = [0.0, 0.0, 0.0, ...]
JOINT_ANGLES_PRE_GRASP: List[float] = [0.0, 0.0, 0.0, ...]
JOINT_ANGLES_GRASP: List[float] = [0.0, 0.0, 0.0, ...]
```

### 关节顺序（10自由度）

```
[0] 拇指俯仰 (Thumb_Pitch)    - 拇指根部弯曲
[1] 拇指偏航 (Thumb_Yaw)      - 拇指旋转
[2] 食指俯仰 (Index_Pitch)    - 食指弯曲
[3] 中指俯仰 (Middle_Pitch)   - 中指弯曲
[4] 无名指俯仰 (Ring_Pitch)     - 无名指弯曲
[5] 小指俯仰 (Pinky_Pitch)    - 小指弯曲
[6] 食指侧摆 (Index_Roll)     - 食指侧摆（被动）
[7] 无名指侧摆 (Ring_Roll)      - 无名指侧摆（被动）
[8] 小指侧摆 (Pinky_Roll)     - 小指侧摆（被动）
[9] 拇指侧摆 (Thumb_Roll)     - 拇指侧摆（被动）
```

**单位**：度（不是弧度！）

### 如何配置

1. **手动测试各个姿态** 使用驱动测试脚本
2. **记录角度值** 产生期望姿态的值
3. **填入数组** 在 `vision_controller.py` 中

示例：
```python
JOINT_ANGLES_GRASP: List[float] = [
    45.0,  # [0] 拇指俯仰 - 弯曲拇指
    30.0,  # [1] 拇指偏航 - 旋转拇指
    60.0,  # [2] 食指俯仰 - 弯曲食指
    60.0,  # [3] 中指俯仰 - 弯曲中指
    60.0,  # [4] 无名指俯仰 - 弯曲无名指
    60.0,  # [5] 小指俯仰 - 弯曲小指
    0.0,   # [6] 食指侧摆 - 被动
    0.0,   # [7] 无名指侧摆 - 被动
    0.0,   # [8] 小指侧摆 - 被动
    0.0,   # [9] 拇指侧摆 - 被动
]
```

## 🚀 使用方法

### 基本用法

```bash
# 在模拟模式下运行（无硬件，用于测试）
cd /home/luka/.ssh/VIST
python scripts/run_hand.py --mock

# 使用真实硬件运行
python scripts/run_hand.py
```

### 高级选项

```bash
# 使用不同的摄像头
python scripts/run_hand.py --camera 1

# 更改控制频率
python scripts/run_hand.py --freq 20

# 调试模式（详细日志）
python scripts/run_hand.py --log-level DEBUG

# 自定义CAN配置
python scripts/run_hand.py --can-id 0x27 --can-channel can0
```

### 键盘控制

- **'q'** - 安全退出
- **'r'** - 重置机械手到零位
- **'s'** - 打印系统状态

## 📊 可视化

系统显示：

1. **手部关键点** - MediaPipe骨架叠加
2. **检测状态** - "检测到手部" 或 "未检测到手"
3. **当前状态** - IDLE / PRE_GRASP / GRASP（颜色编码）
4. **捏合比率** - 实时数值
5. **防抖计数器** - 显示转换进度
6. **目标角度** - 前6个活动关节
7. **FPS和统计** - 性能指标

## 🔧 测试流程

### 步骤1：在模拟模式下测试

```bash
python scripts/run_hand.py --mock
```

这将：
- 初始化摄像头和视觉系统
- 显示可视化
- 不发送命令到硬件
- 用于测试视觉管道

### 步骤2：单独测试驱动

```bash
cd /home/luka/.ssh/VIST
python -m src.robot.hand_driver --real
```

这将：
- 测试硬件通信
- 运行预定义的动作
- 验证CAN总线连接

### 步骤3：校准关节角度

1. 使用驱动测试脚本找到每个姿态的合适角度
2. 更新 `vision_controller.py` 中的数组
3. 单独测试每个状态

### 步骤4：运行完整系统

```bash
python scripts/run_hand.py
```

## 🐛 故障排除

### 摄像头问题

```bash
# 列出可用摄像头
ls /dev/video*

# 测试摄像头
python scripts/run_hand.py --camera 0 --mock
```

### CAN总线问题

```bash
# 检查CAN接口
ip link show can0

# 启动CAN接口（如果需要）
sudo ip link set can0 up type can bitrate 1000000
```

### 导入错误

确保从项目根目录运行：
```bash
cd /home/luka/.ssh/VIST
python scripts/run_hand.py
```

脚本会自动将项目根目录添加到 `sys.path`。

### MediaPipe未找到

```bash
pip install mediapipe opencv-python numpy
```

## 📁 文件结构

```
VIST/
├── src/
│   ├── core/
│   │   ├── __init__.py
│   │   └── vision_controller.py    # ← 视觉 + 状态机
│   └── robot/
│       └── hand_driver.py           # ← 硬件驱动
├── scripts/
│   └── run_hand.py                  # ← 主入口
└── config/
    └── linkerhand_l10_right.urdf    # ← 机器人描述
```

## 🎓 关键概念

### 为什么使用归一化的捏合比率？

使用绝对距离会在以下情况失败：
- 用户离摄像头更近/更远
- 不同的手部大小
- 摄像头缩放变化

归一化比率对这些变化具有鲁棒性。

### 为什么需要防抖？

没有防抖：
- 噪声检测导致快速状态变化
- 手在状态之间抖动
- 控制不舒适

有5帧防抖：
- 平滑、有意的转换
- 更可预测的行为
- 更好的用户体验

### 为什么使用状态机？

而不是连续映射：
- **离散状态**更容易控制
- **可预测的行为** - 你知道每个手势做什么
- **易于扩展** - 以后添加更多状态/手势
- **鲁棒** - 对噪声不太敏感

## 📝 下一步

1. **填入关节角度** 在 `vision_controller.py` 中
2. **在模拟模式下测试** 验证视觉管道
3. **使用硬件测试** 验证动作
4. **调整阈值** 如果需要（比率阈值、防抖帧数）
5. **添加更多状态** 如果需要（例如，指向手势）

## 🔍 代码质量

- ✅ 全程类型提示
- ✅ 全面的文档字符串
- ✅ 适当级别的日志
- ✅ 退出时安全清理
- ✅ 错误处理
- ✅ 命令行界面

## 📞 支持

如果遇到问题：
1. 检查日志（使用 `--log-level DEBUG`）
2. 单独测试组件
3. 验证硬件连接
4. 检查摄像头和CAN总线

---

**创建时间**：2026-02-05
**作者**：Claude (Anthropic)
**项目**：VIST - 基于视觉的手部控制
