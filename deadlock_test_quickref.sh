#!/bin/bash
# 死锁测试 - 快速参考

cat << 'EOF'
╔══════════════════════════════════════════════════════════════╗
║           VIST 死锁测试 - 快速参考卡                        ║
╚══════════════════════════════════════════════════════════════╝

📋 测试目的
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
诊断抖动来源：外部输入 vs 算法内部

🔧 启用测试
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. 编辑: ros2_ws/src/core/vist_kalman_filter.py
2. 找到: update() 方法（约第560行）
3. 取消注释:
   # ENABLE_DEADLOCK_TEST = True  →  ENABLE_DEADLOCK_TEST = True
   # if ENABLE_DEADLOCK_TEST:      →  if ENABLE_DEADLOCK_TEST:
   (取消后续7行的注释)

4. 重启节点:
   pkill -f vist_filter_node
   source ros2_ws/install/setup.bash
   python3 ros2_ws/src/nodes/vist_filter_node.py \
       --ros-args --params-file config/baseline_filters_config.yaml &

📊 诊断结果
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─ 情况A: 不抖了 ✅ ─────────────────────────────────────────┐
│ 结论: 抖动来自外部输入或α跳变                              │
│                                                             │
│ 可能原因:                                                   │
│  • 遥操臂信号太脏（高频噪声）                              │
│  • α在0和1之间疯狂跳变                                     │
│  • 虚拟引导计算不稳定                                       │
│                                                             │
│ 解决方案:                                                   │
│  1. 增加α平滑: intent_smoothing: 0.95                     │
│  2. 放宽约束: cons_variance_xyz: [0.2, 0.2, 1.0]          │
│  3. 增加输入滤波                                            │
└─────────────────────────────────────────────────────────────┘

┌─ 情况B: 还在抖 ❌ ─────────────────────────────────────────┐
│ 结论: 抖动来自算法内部（自激震荡）                         │
│                                                             │
│ 可能原因:                                                   │
│  • 卡尔曼增益过大                                           │
│  • 约束方差过紧（0.01太小）                                │
│  • 过程噪声设置不当                                         │
│                                                             │
│ 解决方案:                                                   │
│  1. 放宽约束: cons_variance_xyz: [0.1, 0.1, 1.0]          │
│  2. 增加过程噪声: position_variance: 1e-3                  │
│  3. 增加观测噪声: human_base_variance: 1e-1                │
└─────────────────────────────────────────────────────────────┘

🔍 验证方法
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 检查α是否锁定为1.0
ros2 topic echo /vist_intent_factors

# 查看测试日志
tail -f /tmp/vist_deadlock_test.log | grep DEADLOCK

⚠️  测试完成后记得禁用！
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
将 ENABLE_DEADLOCK_TEST = True 改回 False
或注释掉整个 if ENABLE_DEADLOCK_TEST: 块

📚 详细文档
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
docs/DEADLOCK_TEST.md

EOF